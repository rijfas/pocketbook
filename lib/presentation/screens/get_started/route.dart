import 'package:flutter/material.dart';

import 'view.dart';

class GetStarted extends MaterialPageRoute {
  GetStarted({Key? key})
      : super(
          builder: (_) => const GetStartedView(),
        );
}

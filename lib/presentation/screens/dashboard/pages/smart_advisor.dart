import 'package:flutter/material.dart';

class SmartAdvisorPage extends StatelessWidget {
  const SmartAdvisorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SmartAdvisor'),
    );
  }
}
